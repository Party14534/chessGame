#include "getEval.hpp"

void getEval(std::string fen){

    

    /*std::string fenCommand = "d";
    char* fenCmd = new char[fenCommand.length() + 1];
    strcpy(fenCmd, fenCommand.c_str());
    system(fenCmd);*/

    /*std::array<char, 128> buffer;
    std::string result;
    std::unique_ptr<FILE, decltype(&pclose)> pipe(popen("eval", "r"), pclose);
    if(!pipe){
        throw std::runtime_error("popen() failed!");
    }
    while (fgets(buffer.data(), buffer.size(), pipe.get()) != nullptr) {
        result += buffer.data();
    }
    std::cout << result << "\n";*/
    //system("quit");

}